import { CustomTextBlockData } from "custom_text.slint";
import { Button, Slider, ComboBox, LineEdit, ScrollView, CheckBox } from "std-widgets.slint";

component StyleVisualIcon inherits Rectangle {
    in property <string> style-id: "";
    in property <color> icon-color: #ffd700;

    width: 24px;
    height: 18px;

    if root.style-id == "spectrum" || root.style-id == "smoothSpectrum" || root.style-id == "waveformBarcodePulse" : HorizontalLayout {
        spacing: 2px;
        alignment: center;
        cross-axis-alignment: end;
        Rectangle { width: 3px; height: 10px; border-radius: 1px; background: root.icon-color; }
        Rectangle { width: 3px; height: 16px; border-radius: 1px; background: root.icon-color; }
        Rectangle { width: 3px; height: 8px; border-radius: 1px; background: root.icon-color; }
        Rectangle { width: 3px; height: 14px; border-radius: 1px; background: root.icon-color; }
    }

    if root.style-id == "radial" || root.style-id == "circularBars" || root.style-id == "pulseRings" || root.style-id == "radialRipple3D" || root.style-id == "vinylRecord" || root.style-id == "orbitSpike" || root.style-id == "cyberRing3D" || root.style-id == "dualPortalBridge" || root.style-id == "cyberBlackHole" || root.style-id == "supernovaBurst" || root.style-id == "quantumEye" || root.style-id == "chronoReactor" || root.style-id == "solarFlareCrown" || root.style-id == "warpDrive" || root.style-id == "neonBiohazard" || root.style-id == "neonLotus" || root.style-id == "holographicVinyl" || root.style-id == "acousticCymascope" || root.style-id == "waveformOscillatingRings" : HorizontalLayout {
        alignment: center;
        cross-axis-alignment: center;
        Rectangle {
            width: 16px;
            height: 16px;
            border-radius: 8px;
            border-width: 2px;
            border-color: root.icon-color;
            Rectangle {
                width: 6px;
                height: 6px;
                border-radius: 3px;
                background: root.icon-color;
            }
        }
    }

    if root.style-id == "oscilloscope" || root.style-id == "minimal" || root.style-id == "auroraWave" || root.style-id == "particleWave3D" || root.style-id == "waterfall3D" || root.style-id == "dualWaveHorizon" || root.style-id == "mercuryFluid" || root.style-id == "quantumRibbon" || root.style-id == "waveformSeismograph" || root.style-id == "waveformDualTube" || root.style-id == "waveformSineComb" || root.style-id == "waveformHarmonicWeb" || root.style-id == "waveformTopographicRibbon" || root.style-id == "waveformCurtainBeams" : HorizontalLayout {
        spacing: 1px;
        alignment: center;
        cross-axis-alignment: center;
        Rectangle { width: 4px; height: 2px; background: root.icon-color; }
        Rectangle { width: 4px; height: 8px; background: root.icon-color; }
        Rectangle { width: 4px; height: 14px; background: root.icon-color; }
        Rectangle { width: 4px; height: 6px; background: root.icon-color; }
        Rectangle { width: 4px; height: 2px; background: root.icon-color; }
    }

    if root.style-id == "equalizer" || root.style-id == "vuMeter" || root.style-id == "laserWall" || root.style-id == "waveformVoxelTerrain" || root.style-id == "waveformSteppedArcade" : HorizontalLayout {
        alignment: center;
        cross-axis-alignment: center;
        GridLayout {
            spacing: 2px;
            Row {
                Rectangle { width: 4px; height: 4px; border-radius: 1px; background: root.icon-color; }
                Rectangle { width: 4px; height: 4px; border-radius: 1px; background: root.icon-color; }
                Rectangle { width: 4px; height: 4px; border-radius: 1px; background: root.icon-color; }
            }
            Row {
                Rectangle { width: 4px; height: 4px; border-radius: 1px; background: root.icon-color; }
                Rectangle { width: 4px; height: 4px; border-radius: 1px; background: root.icon-color; }
                Rectangle { width: 4px; height: 4px; border-radius: 1px; background: root.icon-color; }
            }
        }
    }

    if root.style-id == "waveformFill" || root.style-id == "api3D" : HorizontalLayout {
        spacing: 2px;
        alignment: center;
        cross-axis-alignment: end;
        Rectangle { width: 4px; height: 6px; background: root.icon-color; }
        Rectangle { width: 4px; height: 12px; background: root.icon-color; }
        Rectangle { width: 4px; height: 18px; background: root.icon-color; }
        Rectangle { width: 4px; height: 10px; background: root.icon-color; }
    }

    // Default 3D / Audio Stage icon for all other styles
    if root.style-id != "spectrum" && root.style-id != "smoothSpectrum" && root.style-id != "waveformBarcodePulse" && root.style-id != "radial" && root.style-id != "circularBars" && root.style-id != "pulseRings" && root.style-id != "radialRipple3D" && root.style-id != "vinylRecord" && root.style-id != "orbitSpike" && root.style-id != "cyberRing3D" && root.style-id != "dualPortalBridge" && root.style-id != "cyberBlackHole" && root.style-id != "supernovaBurst" && root.style-id != "quantumEye" && root.style-id != "chronoReactor" && root.style-id != "solarFlareCrown" && root.style-id != "warpDrive" && root.style-id != "neonBiohazard" && root.style-id != "neonLotus" && root.style-id != "holographicVinyl" && root.style-id != "acousticCymascope" && root.style-id != "waveformOscillatingRings" && root.style-id != "oscilloscope" && root.style-id != "minimal" && root.style-id != "auroraWave" && root.style-id != "particleWave3D" && root.style-id != "waterfall3D" && root.style-id != "dualWaveHorizon" && root.style-id != "mercuryFluid" && root.style-id != "quantumRibbon" && root.style-id != "waveformSeismograph" && root.style-id != "waveformDualTube" && root.style-id != "waveformSineComb" && root.style-id != "waveformHarmonicWeb" && root.style-id != "waveformTopographicRibbon" && root.style-id != "waveformCurtainBeams" && root.style-id != "equalizer" && root.style-id != "vuMeter" && root.style-id != "laserWall" && root.style-id != "waveformVoxelTerrain" && root.style-id != "waveformSteppedArcade" && root.style-id != "waveformFill" && root.style-id != "api3D" : HorizontalLayout {
        spacing: 3px;
        alignment: center;
        cross-axis-alignment: center;
        Rectangle { width: 5px; height: 12px; border-radius: 1px; background: root.icon-color; }
        Rectangle { width: 5px; height: 16px; border-radius: 1px; background: root.icon-color; }
        Rectangle { width: 5px; height: 10px; border-radius: 1px; background: root.icon-color; }
    }
}

// Legacy .style-card: 2-column grid card with icon + label, gold-tinted on black.
// Micro-interactions: subtle scale + gold glow on hover, stronger glow when
// selected (legacy `box-shadow: 0 0 12px rgba(...,0.2)` + border gold).
component StyleCard inherits Rectangle {
    in property <string> style-id: "";
    in property <string> label: "";
    in property <string> current: "";
    in-out property <bool> has-hover: ta.has-hover;
    callback style-clicked(string);

    height: 56px;

    ta := TouchArea {
        width: parent.width;
        height: parent.height;
        mouse-cursor: pointer; // legacy .style-card { cursor: pointer }
        clicked => { root.style-clicked(root.style-id); }

        Rectangle {
            width: parent.width;
            height: parent.height;
            border-radius: 8px;
            border-width: 1px;
            border-color: root.current == root.style-id ? #ffd700 : (root.has-hover ? #ffd70066 : #ffd70029);
            background: root.current == root.style-id ? #ffd7001a : (root.has-hover ? #ffd70014 : #ffd70008);

            // Hover: lift + glow; selected: persistent glow (legacy box-shadow).
            // `transform-scale-*` is a builtin reserved property (1.0 = normal).
            transform-scale-x: root.has-hover ? 1.04 : 1.0;
            transform-scale-y: root.has-hover ? 1.04 : 1.0;
            transform-origin: { x: self.width / 2, y: self.height / 2 };
            drop-shadow-blur: root.current == root.style-id ? 12px : (root.has-hover ? 8px : 0px);
            drop-shadow-color: #ffd70066;
            animate transform-scale-x { duration: 150ms; easing: ease-out; }
            animate transform-scale-y { duration: 150ms; easing: ease-out; }
            animate drop-shadow-blur { duration: 150ms; easing: ease-out; }

            VerticalLayout {
                padding: 4px;
                spacing: 4px;
                alignment: center;

                HorizontalLayout {
                    alignment: center;
                    StyleVisualIcon {
                        style-id: root.style-id;
                        icon-color: root.current == root.style-id ? #ffd700 : (root.has-hover ? #f8fafc : #a3a3a3);
                    }
                }

                Text {
                    text: root.label;
                    color: root.current == root.style-id ? #ffd700 : (root.has-hover ? #f8fafc : #a3a3a3);
                    font-size: 11px;
                    font-weight: 600;
                    horizontal-alignment: center;
                }
            }
        }
    }
}

// Compact segmented tab button (legacy pill-style tab bar): equal-width cells
// with a gold active state, denser than the default widget buttons.
// Micro-interactions: subtle glow + lift on hover; active tab keeps a soft gold
// glow so the current tab reads at a glance (legacy active underline + tint).
component TabBtn inherits Rectangle {
    in property <string> label: "";
    in property <bool> active: false;
    callback clicked();

    height: 30px;
    horizontal-stretch: 1;
    border-radius: 6px;
    background: root.active ? #ffd700 : (ta.has-hover ? #ffffff14 : #ffffff06);

        transform-scale-x: ta.has-hover && !root.active ? 1.05 : 1.0;
        transform-scale-y: ta.has-hover && !root.active ? 1.05 : 1.0;
        transform-origin: { x: self.width / 2, y: self.height / 2 };
        drop-shadow-blur: root.active ? 10px : (ta.has-hover ? 6px : 0px);
        drop-shadow-color: root.active ? #ffd70080 : #ffd70055;
        animate transform-scale-x { duration: 120ms; easing: ease-out; }
        animate transform-scale-y { duration: 120ms; easing: ease-out; }
        animate drop-shadow-blur { duration: 120ms; easing: ease-out; }

        ta := TouchArea {
            width: parent.width;
            height: parent.height;
            mouse-cursor: pointer;
            clicked => { root.clicked(); }
        }

        Text {
            text: root.label;
            color: root.active ? #000000 : (ta.has-hover ? #f8fafc : #a3a3a3);
            font-size: 11px;
            font-weight: 600;
            horizontal-alignment: center;
            vertical-alignment: center;
        }

        Tooltip {
            Text { text: root.label; font-size: 11px; color: #f8fafc; }
        }
}

// Legacy `.section-title`: uppercase, bold, muted, letter-spaced. A single line
// with a trailing gold hairline keeps the panel tidy and scannable.
component SectionTitle {
    in property <string> text: "";

    HorizontalLayout {
        spacing: 8px;
        alignment: center;
        cross-axis-alignment: center;

        Text {
            text: root.text;
            color: #a3a3a3;
            font-size: 12px;
            font-weight: 700;
            letter-spacing: 0.5px;
        }

        Rectangle {
            horizontal-stretch: 1;
            height: 1px;
            background: #ffffff14;
        }
    }
}

// Compact slider row: label on the left, live value on the right (gold), track
// below. Replaces the previous label-above-track layout that wasted a row per
// control and made the panel feel cluttered.
component SliderRow {
    in property <string> label: "";
    in property <string> value-text: "";
    in property <float> minimum: 0.0;
    in property <float> maximum: 1.0;
    // `value` is deliberately `in`-only: callers bind it one-way (`value: root.x`)
    // and update the same property in their `changed` handler, so the binding
    // stays live and the slider re-syncs — writing back here would break it.
    in property <float> value: 0.0;
    callback changed(float);

    VerticalLayout {
        spacing: 2px;

        HorizontalLayout {
            spacing: 6px;

            Text {
                text: root.label;
                color: #a3a3a3;
                font-size: 11px;
                vertical-alignment: center;
            }

            Rectangle { horizontal-stretch: 1; }

            Text {
                text: root.value-text;
                color: #ffd700;
                font-size: 11px;
                font-weight: 600;
                vertical-alignment: center;
            }
        }

        Slider {
            minimum: root.minimum;
            maximum: root.maximum;
            value: root.value;
            changed(val) => { root.changed(val); }
        }
    }
}


component ColorCanvas2D inherits Rectangle {
    in-out property <string> hex-val: "#ff0000";
    in-out property <float> hue-pos: 0.0;
    in-out property <float> sat-x: 0.9;
    in-out property <float> val-y: 0.1;
    callback changed(string);

    // Live color computed from the pointer state (HSV -> RGB), used by the
    // preview badge and rendered inside the sat/val box. Degrees for `hsv()`
    // are 0..360, s/v/a are 0..1.
    in-out property <color> spectrum-color:
        root.val-y > 0.90 ? #000000
        : root.sat-x < 0.10 && root.val-y < 0.10 ? #ffffff
        : hsv(root.hue-pos * 360.0, root.sat-x.clamp(0.0, 1.0), (1.0 - root.val-y).clamp(0.0, 1.0), 1.0);

    height: 160px;

    HorizontalLayout {
        spacing: 10px;

        // 1. 2D SATURATION & BRIGHTNESS GRADIENT CANVAS
        sat-val-box := Rectangle {
            horizontal-stretch: 1;
            height: 160px;
            border-radius: 8px;
            border-width: 1px;
            border-color: #ffffff33;
            clip: true;

            // Pure Hue Base Background Color — continuous hue (not stepped
            // bands) so dragging is smooth and matches the computed RGB above.
            background: hsv(root.hue-pos * 360.0, 1.0, 1.0);

            // Saturation Overlay: White to Transparent (Left to Right)
            Rectangle {
                width: parent.width;
                height: parent.height;
                background: @linear-gradient(90deg, #ffffff 0%, #ffffff00 100%);
            }

            // Value/Brightness Overlay: Transparent to Black (Top to Bottom)
            Rectangle {
                width: parent.width;
                height: parent.height;
                background: @linear-gradient(180deg, #00000000 0%, #000000 100%);
            }

            // 2D TouchArea
            sat-ta := TouchArea {
                width: parent.width;
                height: parent.height;
                mouse-cursor: pointer;
                moved => {
                    if (self.pressed) {
                        root.sat-x = (self.mouse-x / self.width).clamp(0.0, 1.0);
                        root.val-y = (self.mouse-y / self.height).clamp(0.0, 1.0);
                        root.update-color();
                    }
                }
                pointer-event(evt) => {
                    if (evt.kind == PointerEventKind.down) {
                        root.sat-x = (self.mouse-x / self.width).clamp(0.0, 1.0);
                        root.val-y = (self.mouse-y / self.height).clamp(0.0, 1.0);
                        root.update-color();
                    }
                }
            }

            // Circular Drag Handle Ring ⭕ (Left 2D Box)
            Rectangle {
                x: (root.sat-x * parent.width - 8px).clamp(0px, parent.width - 16px);
                y: (root.val-y * parent.height - 8px).clamp(0px, parent.height - 16px);
                width: 16px;
                height: 16px;
                border-radius: 8px;
                border-width: 3px;
                border-color: #ffffff;
                drop-shadow-blur: 4px;
                drop-shadow-color: #000000aa;
            }
        }

        // 2. VERTICAL HUE SPECTRUM RAINBOW SLIDER BAR
        hue-bar := Rectangle {
            width: 22px;
            height: 160px;
            border-radius: 11px;
            border-width: 1px;
            border-color: #ffffff33;
            clip: true;
            background: @linear-gradient(180deg, #ff0000 0%, #ffff00 17%, #00ff00 33%, #00ffff 50%, #0000ff 67%, #ff00ff 83%, #ff0000 100%);

            hue-ta := TouchArea {
                width: parent.width;
                height: parent.height;
                mouse-cursor: pointer;
                moved => {
                    if (self.pressed) {
                        root.hue-pos = (self.mouse-y / self.height).clamp(0.0, 1.0);
                        root.update-color();
                    }
                }
                pointer-event(evt) => {
                    if (evt.kind == PointerEventKind.down) {
                        root.hue-pos = (self.mouse-y / self.height).clamp(0.0, 1.0);
                        root.update-color();
                    }
                }
            }

            // Circular Drag Handle Ring ⭕ (Right Vertical Bar)
            Rectangle {
                x: 2px;
                y: (root.hue-pos * (parent.height - 18px)).clamp(0px, parent.height - 18px);
                width: 18px;
                height: 18px;
                border-radius: 9px;
                border-width: 3px;
                border-color: #ffffff;
                drop-shadow-blur: 4px;
                drop-shadow-color: #000000aa;
            }
        }
    }

    // ---- Accurate HSV -> RGB -> hex conversion ----
    // hue-pos in 0..1 -> degrees for HSV; sat-x = saturation (0=right edge
    // white, 1=full), val-y = bottom-black (value = 1 - val-y).
    function ch-rr(c: float, x: float, m: float, i: float) -> float {
        return (i == 0 ? c : i == 1 ? x : i == 2 ? 0.0 : i == 3 ? 0.0 : i == 4 ? x : c) + m;
    }
    function ch-gg(c: float, x: float, m: float, i: float) -> float {
        return (i == 0 ? x : i == 1 ? c : i == 2 ? c : i == 3 ? x : i == 4 ? 0.0 : 0.0) + m;
    }
    function ch-bb(c: float, x: float, m: float, i: float) -> float {
        return (i == 0 ? 0.0 : i == 1 ? 0.0 : i == 2 ? x : i == 3 ? c : i == 4 ? c : x) + m;
    }
    function clamp-01(v: float) -> float { return v < 0.0 ? 0.0 : v > 1.0 ? 1.0 : v; }

    // Integer 0..255 -> two hex chars (#RRGGBB).
    function hex-digit(d: float) -> string {
        return d == 0 ? "0" : d == 1 ? "1" : d == 2 ? "2" : d == 3 ? "3" : d == 4 ? "4"
            : d == 5 ? "5" : d == 6 ? "6" : d == 7 ? "7" : d == 8 ? "8" : d == 9 ? "9"
            : d == 10 ? "a" : d == 11 ? "b" : d == 12 ? "c" : d == 13 ? "d" : d == 14 ? "e" : "f";
    }
    function hex2(v: float) -> string {
        let n = Math.round(v);
        return hex-digit(Math.floor(n / 16.0)) + hex-digit(n - Math.floor(n / 16.0) * 16.0);
    }

    function update-color() {
        // Corner snaps: pure black at very bottom, pure white at very top-left.
        if (root.val-y > 0.94) {
            root.hex-val = "#000000";
        } else if (root.sat-x < 0.08 && root.val-y < 0.08) {
            root.hex-val = "#ffffff";
        } else {
            let v = 1.0 - root.val-y;
            let s = root.sat-x;
            let h = root.hue-pos * 360.0;
            let c = v * s;
            let hp = h / 60.0;
            let sect = Math.floor(hp);
            let x = c * (1.0 - Math.abs(Math.mod(hp, 2.0) - 1.0));
            let m = v - c;
            let r = root.clamp-01(root.ch-rr(c, x, m, sect));
            let g = root.clamp-01(root.ch-gg(c, x, m, sect));
            let b = root.clamp-01(root.ch-bb(c, x, m, sect));
            root.hex-val = "#" + hex2(r * 255.0) + hex2(g * 255.0) + hex2(b * 255.0);
        }
        root.changed(root.hex-val);
    }
}

component ColorPickerField inherits Rectangle {
    in property <string> label: "";
    in-out property <string> hex-val: "#00f0ff";
    in-out property <bool> show-canvas: true;
    callback changed(string);

    background: #ffffff05;
    border-radius: 8px;
    border-width: 1px;
    border-color: #ffffff14;

    VerticalLayout {
        padding: 10px;
        spacing: 8px;

        // Label Header & Live Visual Color Box Preview
        HorizontalLayout {
            spacing: 8px;
            cross-axis-alignment: center;

            Text {
                text: root.label;
                color: #f1f5f9;
                font-size: 11px;
                font-weight: 700;
            }

            Rectangle { horizontal-stretch: 1; }

            // Live Color Box Badge — reflects the CURRENT color. Known preset hex
// strings map to a real color; for any other (custom/typed) value the badge
// tracks the live HSV->RGB color the user is dragging on the canvas (a plain
// `#RRGGBB` string cannot be re-parsed to a color at runtime in Slint).
            Rectangle {
                width: 26px;
                height: 20px;
                border-radius: 5px;
                border-width: 1px;
                border-color: #ffffff55;
                background: root.hex-val == "#00f0ff" ? #00f0ff
                          : root.hex-val == "#ff007f" ? #ff007f
                          : root.hex-val == "#ffe600" ? #ffe600
                          : root.hex-val == "#9333ea" ? #9333ea
                          : root.hex-val == "#10b981" ? #10b981
                          : root.hex-val == "#ef4444" ? #ef4444
                          : root.hex-val == "#f97316" ? #f97316
                          : root.hex-val == "#3b82f6" ? #3b82f6
                          : root.hex-val == "#ec4899" ? #ec4899
                          : root.hex-val == "#00ff99" ? #00ff99
                          : root.hex-val == "#7c3aed" ? #7c3aed
                          : root.hex-val == "#f59e0b" ? #f59e0b
                          : root.hex-val == "#ff6b6b" ? #ff6b6b
                          : root.hex-val == "#84cc16" ? #84cc16
                          : root.hex-val == "#ffffff" ? #ffffff
                          : root.hex-val == "#000000" ? #000000
                          : color-canvas-badge.spectrum-color;
            }
        }

        // FULL 2D COLOR PICKER CANVAS (Sat/Val 2D Box + Vertical Rainbow Bar)
        color-canvas-badge := ColorCanvas2D {
            hex-val: root.hex-val;
            changed(c) => {
                root.hex-val = c;
                root.changed(c);
            }
        }

        // Custom Hex Input Field & Direct Editing
        HorizontalLayout {
            spacing: 6px;

            Text {
                text: "Hex:";
                color: #94a3b8;
                font-size: 11px;
                font-weight: 600;
                vertical-alignment: center;
            }

            LineEdit {
                horizontal-stretch: 1;
                text: root.hex-val;
                placeholder-text: "#RRGGBB";
                edited(val) => {
                    root.hex-val = val;
                    root.changed(val);
                }
            }
        }
    }
}

export component ControlPanel inherits Rectangle {
    width: 380px; // legacy .control-panel width
    background: #0a0a0a;

    in-out property <string> active-tab: "style";
    in-out property <string> style-val: "spectrum";
    in-out property <int> bar-count: 64;
    in-out property <float> bar-gap: 2.0;
    in-out property <float> bar-width: 6.0;
    in-out property <float> bar-rounding: 2.0;
    in-out property <bool> mirror-bars: false;
    in-out property <float> sensitivity: 1.2;
    in-out property <float> smoothing: 0.8;
    in-out property <float> bass-multiplier: 1.5;
    in-out property <float> fire-width-ratio: 0.94;
    in-out property <float> fire-height-scale: 1.0;
    in-out property <string> radial-center-image-uri: "";
    in-out property <float> visualizer-scale: 1.0;
    in-out property <float> position-x: 0.0;
    in-out property <float> position-y: 0.0;

    in-out property <string> theme-name: "Cyberpunk";
    in-out property <string> primary-color: "#00f0ff";
    in-out property <string> secondary-color: "#ff007f";
    in-out property <string> accent-color: "#ffe600";
    in-out property <string> glow-color: "#00f0ff";
    in-out property <bool> show-peaks: false;
    in-out property <string> peak-color: "#ffffff";

    in-out property <string> bg-mode: "Gradient";
    in-out property <string> solid-color: "#090a0f";
    in-out property <string> gradient-start: "#0f0c20";
    in-out property <string> gradient-end: "#06101e";
    in-out property <float> blur-amount: 0.0;
    in-out property <float> overlay-opacity: 0.3;
    in-out property <float> image-opacity: 1.0;
    in-out property <string> custom-bg-name: "";
    in-out property <bool> show-particles: true;
    in-out property <bool> show-music-notes: false;
    in-out property <bool> show-fireworks: false;
    in-out property <bool> show-matrix-rain: false;
    in-out property <bool> show-fireflies: false;
    in-out property <bool> show-sakura: false;
    in-out property <bool> show-cyber-lightning: false;
    in-out property <float> grid-size: 40.0;
    in-out property <float> grid-line-width: 1.0;
    in-out property <string> grid-color: "#ffffff";
    in-out property <string> particle-style: "Float";
    in-out property <float> particle-size: 4.0;
    in-out property <float> particle-speed: 1.0;
    in-out property <float> particle-count: 60.0;
    in-out property <string> particle-color: "#00f0ff";
    in-out property <float> fireworks-count: 7.0;
    in-out property <float> fireworks-size: 4.0;
    in-out property <float> fireworks-speed: 1.0;
    in-out property <float> fireworks-depth: 1.0;
    in-out property <string> fireworks-color: "#ff7700";
    in-out property <float> matrix-rain-count: 40.0;
    in-out property <float> matrix-rain-size: 5.0;
    in-out property <float> matrix-rain-speed: 1.0;
    in-out property <float> matrix-rain-depth: 1.0;
    in-out property <string> matrix-rain-color: "#00ff66";
    in-out property <float> fireflies-count: 36.0;
    in-out property <float> fireflies-size: 6.0;
    in-out property <float> fireflies-speed: 1.0;
    in-out property <float> fireflies-depth: 1.0;
    in-out property <string> fireflies-color: "#ffcc00";
    in-out property <float> sakura-count: 28.0;
    in-out property <float> sakura-size: 10.0;
    in-out property <float> sakura-speed: 1.0;
    in-out property <float> sakura-depth: 1.0;
    in-out property <string> sakura-color: "#ffb7c5";
    in-out property <float> cyber-lightning-count: 4.0;
    in-out property <float> cyber-lightning-size: 3.0;
    in-out property <float> cyber-lightning-speed: 1.0;
    in-out property <float> cyber-lightning-depth: 1.0;
    in-out property <string> cyber-lightning-color: "#00f0ff";
    in-out property <string> music-note-style: "Float";
    in-out property <float> music-note-size: 60.0;
    in-out property <float> music-note-count: 80.0;
    in-out property <float> music-note-sensitivity: 1.0;
    in-out property <string> music-note-color: "#ffe600";
    in-out property <float> star-count: 160.0;
    in-out property <float> star-speed: 1.0;
    in-out property <float> star-brightness: 1.0;
    in-out property <float> nebula-intensity: 0.6;
    in-out property <float> nebula-speed: 1.0;
    in-out property <float> aurora-speed: 1.0;
    in-out property <float> aurora-amplitude: 50.0;
    in-out property <float> aurora-opacity: 0.25;
    in-out property <float> grain-opacity: 0.08;
    in-out property <float> bokeh-count: 18.0;
    in-out property <float> bokeh-size: 30.0;
    in-out property <float> bokeh-opacity: 0.3;
    in-out property <float> psychedelic-speed: 1.0;
    in-out property <float> psychedelic-bands: 24.0;
    in-out property <float> psychedelic-line-width: 4.0;

    in-out property <string> main-effect: "None";
    in-out property <bool> effects-enabled: true;
    in-out property <float> shake-intensity: 1.0;
    in-out property <float> shake-frequency: 0.5;
    in-out property <float> shake-max-offset: 40.0;
    in-out property <bool> shake-on-beat: false;
    in-out property <float> glitch-intensity: 0.5;
    in-out property <float> pulse-intensity: 0.7;
    in-out property <float> vignette-intensity: 0.7;
    in-out property <string> spotlight-color: "#ffd700";
    in-out property <float> strobe-intensity: 0.8;
    in-out property <float> scanline-opacity: 0.2;
    in-out property <float> chromatic-intensity: 0.5;
    in-out property <float> zoom-intensity: 0.2;
    in-out property <float> invert-intensity: 0.5;
    in-out property <float> bars-amount: 0.6;
    in-out property <float> shockwave-intensity: 0.6;
    in-out property <float> pixelate-intensity: 0.6;
    in-out property <float> tilt-intensity: 0.6;
    in-out property <float> heat-haze-intensity: 0.6;
    in-out property <float> hue-shift-intensity: 0.6;
    in-out property <bool> bg-only-effect: true;

    in-out property <string> track-title: "";
    in-out property <string> track-artist: "";
    in-out property <string> cassette-label: "AUDIOWAVE VOLUME #1";
    in-out property <bool> show-title: true;
    in-out property <bool> show-artist: true;
    in-out property <string> text-preset: "Bottom Center";
    in-out property <float> title-pos-x: 50.0;
    in-out property <float> title-pos-y: 78.0;
    in-out property <float> title-font-size: 36.0;
    in-out property <string> title-color: "#ffffff";
    in-out property <float> artist-pos-x: 50.0;
    in-out property <float> artist-pos-y: 86.0;
    in-out property <float> artist-font-size: 20.0;
    in-out property <string> artist-color: "#a3a3a3";
    in-out property <[CustomTextBlockData]> custom-text-blocks: [];
    callback add-custom-text-clicked();
    callback remove-custom-text-clicked(int);
    callback custom-text-edited(int, string, bool, float, float, float, string, string, string);

    callback config-changed();
    callback theme-preset-selected(string);
    callback open-radial-image-clicked();
    callback remove-radial-image-clicked();
    callback open-custom-image-clicked();
    callback remove-custom-image-clicked();

    VerticalLayout {
        padding: 10px;
        spacing: 8px;

        // TAB BAR — compact segmented pills (legacy .tabs)
        HorizontalLayout {
            spacing: 4px;

            TabBtn { label: "Style"; active: root.active-tab == "style"; clicked => { root.active-tab = "style"; } }
            TabBtn { label: "Colors"; active: root.active-tab == "colors"; clicked => { root.active-tab = "colors"; } }
            TabBtn { label: "Bg"; active: root.active-tab == "background"; clicked => { root.active-tab = "background"; } }
            TabBtn { label: "FX"; active: root.active-tab == "effects"; clicked => { root.active-tab = "effects"; } }
            TabBtn { label: "Text"; active: root.active-tab == "text"; clicked => { root.active-tab = "text"; } }
        }

        // TAB CONTENT AREA
        ScrollView {
            vertical-stretch: 1;

            VerticalLayout {
                alignment: start;
                padding: 10px;
                spacing: 12px;

                // STYLE TAB
                if (root.active-tab == "style") : VerticalLayout {
                    spacing: 10px;

                    // 2-column grid of 32 style cards (legacy .style-grid)
                    GridLayout {
                        spacing: 8px;

                        StyleCard { row: 0; col: 0; style-id: "spectrum"; label: "Spectrum Bars"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 0; col: 1; style-id: "radial"; label: "Radial Ring"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 1; col: 0; style-id: "oscilloscope"; label: "Oscilloscope"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 1; col: 1; style-id: "equalizer"; label: "Matrix Equalizer"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 2; col: 0; style-id: "minimal"; label: "Minimal Wave"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 2; col: 1; style-id: "waveformFill"; label: "Waveform Fill"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 3; col: 0; style-id: "circularBars"; label: "Circular Bars"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 3; col: 1; style-id: "smoothSpectrum"; label: "Smooth Spectrum"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 4; col: 0; style-id: "pulseRings"; label: "Pulse Rings"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 4; col: 1; style-id: "vuMeter"; label: "VU Meter"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 5; col: 0; style-id: "auroraWave"; label: "Aurora Wave"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 5; col: 1; style-id: "spiralGalaxy"; label: "Spiral Galaxy"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 6; col: 0; style-id: "threeD"; label: "3D Blocks"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 6; col: 1; style-id: "api3D"; label: "Fire 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 7; col: 0; style-id: "neonCity3D"; label: "Neon City 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 7; col: 1; style-id: "speaker3D"; label: "Speaker 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 8; col: 0; style-id: "speakerTrio"; label: "Speaker Trio"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 8; col: 1; style-id: "speakerSplatter"; label: "Speaker Splatter"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 9; col: 0; style-id: "radialRipple3D"; label: "3D Radial Ripple"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 9; col: 1; style-id: "waterfall3D"; label: "3D Waterfall"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 10; col: 0; style-id: "cassetteTape"; label: "Retro Cassette"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 10; col: 1; style-id: "vinylRecord"; label: "Vinyl Record"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 11; col: 0; style-id: "turntable"; label: "Turntable"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 11; col: 1; style-id: "retroRadio"; label: "Retro Radio"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 12; col: 0; style-id: "djController"; label: "DJ Controller"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 12; col: 1; style-id: "speakerExplosion"; label: "Speaker Explosion"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 13; col: 0; style-id: "orbitSpike"; label: "Orbit Spike"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 13; col: 1; style-id: "cyberRing3D"; label: "Cyber Ring 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 14; col: 0; style-id: "hologramStage"; label: "Hologram Stage"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 14; col: 1; style-id: "dualPortalBridge"; label: "Dual Portal Bridge"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 15; col: 0; style-id: "particleWave3D"; label: "Particle Wave 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 15; col: 1; style-id: "cylinderMatrix3D"; label: "3D Cylinder Matrix"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 16; col: 0; style-id: "cyberHorizon"; label: "Cyber Horizon"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 16; col: 1; style-id: "cyberBlackHole"; label: "Cyber Black Hole"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 17; col: 0; style-id: "supernovaBurst"; label: "Supernova Burst"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 17; col: 1; style-id: "quantumEye"; label: "Quantum Eye"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 18; col: 0; style-id: "chronoReactor"; label: "Chrono Reactor"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 18; col: 1; style-id: "solarFlareCrown"; label: "Solar Flare Crown"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 19; col: 0; style-id: "warpDrive"; label: "Warp Drive"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 19; col: 1; style-id: "neonBiohazard"; label: "Neon Biohazard"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 20; col: 0; style-id: "neonLotus"; label: "Neon Lotus"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 20; col: 1; style-id: "holographicVinyl"; label: "Holographic Vinyl"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 21; col: 0; style-id: "acousticCymascope"; label: "Acoustic Cymascope"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 21; col: 1; style-id: "synthwaveHighway3D"; label: "Synthwave Highway 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 22; col: 0; style-id: "mercuryFluid"; label: "Mercury Fluid Wave"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 22; col: 1; style-id: "neonMetropolis3D"; label: "Neon Metropolis 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 23; col: 0; style-id: "matrixRain"; label: "Matrix Data Rain"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 23; col: 1; style-id: "quantumRibbon"; label: "Quantum Ribbon 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 24; col: 0; style-id: "audioPrism3D"; label: "Audio Prism 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 24; col: 1; style-id: "vaporwaveDeck3D"; label: "Vaporwave Deck 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 25; col: 0; style-id: "nebulaCloud3D"; label: "Nebula Cloud 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 25; col: 1; style-id: "cyberTunnel3D"; label: "Cyber Tunnel 3D"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 26; col: 0; style-id: "laserWall"; label: "Laser Equalizer Wall"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 26; col: 1; style-id: "dualWaveHorizon"; label: "Dual Wave Horizon"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 27; col: 0; style-id: "waveformSeismograph"; label: "3D Seismograph"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 27; col: 1; style-id: "waveformDualTube"; label: "Neon Dual Tube"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 28; col: 0; style-id: "waveformVoxelTerrain"; label: "3D Voxel Terrain"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 28; col: 1; style-id: "waveformSineComb"; label: "Spring Comb Wave"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 29; col: 0; style-id: "waveformHarmonicWeb"; label: "Harmonic String Web"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 29; col: 1; style-id: "waveformSteppedArcade"; label: "8-Bit Stepped Arcade"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 30; col: 0; style-id: "waveformBarcodePulse"; label: "Barcode Pulse Wave"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 30; col: 1; style-id: "waveformCurtainBeams"; label: "Laser Curtain Beams"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }

                        StyleCard { row: 31; col: 0; style-id: "waveformOscillatingRings"; label: "Oscillating Rings"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                        StyleCard { row: 31; col: 1; style-id: "waveformTopographicRibbon"; label: "Topographic Ribbon"; current: root.style-val; style-clicked(id) => { root.style-val = id; root.config-changed(); } }
                    }

                    if (root.style-val == "spectrum" || root.style-val == "smoothSpectrum" || root.style-val == "hologramStage" || root.style-val == "orbitSpike" || root.style-val == "speakerExplosion" || root.style-val == "cyberRing3D" || root.style-val == "cylinderMatrix3D" || root.style-val == "neonCity3D" || root.style-val == "turntable" || root.style-val == "retroRadio") : SliderRow {
                        label: "Bar Count";
                        value-text: "" + root.bar-count;
                        minimum: 16;
                        maximum: 128;
                        value: root.bar-count;
                        changed(val) => { root.bar-count = Math.round(val); root.config-changed(); }
                    }

                    if (root.style-val == "spectrum") : VerticalLayout {
                        spacing: 10px;
                        SliderRow {
                            label: "Bar Gap";
                            value-text: root.bar-gap + "px";
                            minimum: 0;
                            maximum: 20;
                            value: root.bar-gap;
                            changed(val) => { root.bar-gap = Math.round(val); root.config-changed(); }
                        }

                        SliderRow {
                            label: "Bar Width";
                            value-text: root.bar-width + "px";
                            minimum: 2;
                            maximum: 40;
                            value: root.bar-width;
                            changed(val) => { root.bar-width = Math.round(val); root.config-changed(); }
                        }

                        SliderRow {
                            label: "Bar Rounding";
                            value-text: root.bar-rounding + "px";
                            minimum: 0;
                            maximum: 20;
                            value: root.bar-rounding;
                            changed(val) => { root.bar-rounding = Math.round(val); root.config-changed(); }
                        }
                    }

                    if (root.style-val == "spectrum" || root.style-val == "smoothSpectrum" || root.style-val == "equalizer" || root.style-val == "waveformFill") : VerticalLayout {
                        spacing: 10px;
                        CheckBox {
                            text: "Mirror Waves Downwards";
                            checked: root.mirror-bars;
                            toggled => { root.mirror-bars = self.checked; root.config-changed(); }
                        }
                        Text { text: "Reflects the visualization symmetrically below the center line"; color: #737373; font-size: 11px; }
                    }

                    SliderRow {
                        label: "Sensitivity";
                        value-text: "" + (Math.round(root.sensitivity * 10) / 10);
                        minimum: 0.2;
                        maximum: 3.0;
                        value: root.sensitivity;
                        changed(val) => { root.sensitivity = val; root.config-changed(); }
                    }

                    SliderRow {
                        label: "Bass Boost";
                        value-text: "" + (Math.round(root.bass-multiplier * 10) / 10);
                        minimum: 0.5;
                        maximum: 4.0;
                        value: root.bass-multiplier;
                        changed(val) => { root.bass-multiplier = val; root.config-changed(); }
                    }

                    SliderRow {
                        label: "Smoothing";
                        value-text: "" + (Math.round(root.smoothing * 100) / 100);
                        minimum: 0.0;
                        maximum: 0.95;
                        value: root.smoothing;
                        changed(val) => { root.smoothing = val; root.config-changed(); }
                    }

                    if (root.style-val == "api3D") : VerticalLayout {
                        spacing: 10px;
                        SliderRow {
                            label: "Fire Wave Width";
                            value-text: Math.round(root.fire-width-ratio * 100) + "%";
                            minimum: 0.3;
                            maximum: 1.0;
                            value: root.fire-width-ratio;
                            changed(val) => { root.fire-width-ratio = val; root.config-changed(); }
                        }

                        SliderRow {
                            label: "Fire Wave Height";
                            value-text: "" + (Math.round(root.fire-height-scale * 10) / 10) + "x";
                            minimum: 0.3;
                            maximum: 2.5;
                            value: root.fire-height-scale;
                            changed(val) => { root.fire-height-scale = val; root.config-changed(); }
                        }
                    }

                    SliderRow {
                        label: "Visualizer Scale";
                        value-text: "" + (Math.round(root.visualizer-scale * 10) / 10);
                        minimum: 0.2;
                        maximum: 3.0;
                        value: root.visualizer-scale;
                        changed(val) => { root.visualizer-scale = val; root.config-changed(); }
                    }

                    SliderRow {
                        label: "Position X (Left / Right)";
                        value-text: Math.round(root.position-x * 100) + "%";
                        minimum: -1.0;
                        maximum: 1.0;
                        value: root.position-x;
                        changed(val) => { root.position-x = val; root.config-changed(); }
                    }

                    SliderRow {
                        label: "Position Y (Up / Down)";
                        value-text: Math.round(root.position-y * 100) + "%";
                        minimum: -1.0;
                        maximum: 1.0;
                        value: root.position-y;
                        changed(val) => { root.position-y = val; root.config-changed(); }
                    }

                    SectionTitle { text: "RADIAL CENTER IMAGE"; }
                    Button {
                        text: root.radial-center-image-uri == "" ? "Choose Center Image" : "Change Center Image";
                        clicked => { root.open-radial-image-clicked(); }
                    }
                    if (root.radial-center-image-uri != "") : Button {
                        text: "Remove Image";
                        clicked => { root.remove-radial-image-clicked(); }
                    }
                }

                // COLORS TAB
                if (root.active-tab == "colors") : VerticalLayout {
                    spacing: 10px;
                    SectionTitle { text: "COLOR THEME PRESETS"; }
                    ComboBox {
                        height: 34px;
                        model: ["Cyberpunk", "Synthwave", "Emerald", "Violet", "Gold", "Custom"];
                        current-value: root.theme-name;
                        current-index: root.theme-name == "Cyberpunk" ? 0 : root.theme-name == "Synthwave" ? 1 : root.theme-name == "Emerald" ? 2 : root.theme-name == "Violet" ? 3 : root.theme-name == "Gold" ? 4 : 5;
                        selected(val) => { root.theme-name = val; root.theme-preset-selected(val); }
                    }
                    Button {
                        text: "↺ Reset Colors";
                        enabled: root.theme-name != "Custom";
                        clicked => { root.theme-preset-selected(root.theme-name); }
                    }
                    if (root.theme-name == "Custom") : Text {
                        text: "Reset is disabled while Custom is selected";
                        color: #737373;
                        font-size: 11px;
                    }

                    SectionTitle { text: "CUSTOM COLORS & PALETTE"; }

                    ColorPickerField {
                        label: "Primary Color";
                        hex-val: root.primary-color;
                        changed(val) => { root.primary-color = val; root.config-changed(); }
                    }

                    ColorPickerField {
                        label: "Secondary Color";
                        hex-val: root.secondary-color;
                        changed(val) => { root.secondary-color = val; root.config-changed(); }
                    }

                    ColorPickerField {
                        label: "Accent Color";
                        hex-val: root.accent-color;
                        changed(val) => { root.accent-color = val; root.config-changed(); }
                    }

                    ColorPickerField {
                        label: "Glow Color";
                        hex-val: root.glow-color;
                        changed(val) => { root.glow-color = val; root.config-changed(); }
                    }

                    SectionTitle { text: "PEAK MARKERS"; }
                    CheckBox {
                        text: "Show Dynamic Peak Drop Markers";
                        checked: root.show-peaks;
                        toggled => { root.show-peaks = self.checked; root.config-changed(); }
                    }
                    if (root.show-peaks) : VerticalLayout {
                        spacing: 10px;
                        ColorPickerField {
                            label: "Peak Marker Color";
                            hex-val: root.peak-color;
                            changed(val) => { root.peak-color = val; root.config-changed(); }
                        }
                    }
                }

                // BACKGROUND TAB
                if (root.active-tab == "background") : VerticalLayout {
                    spacing: 10px;
                    SectionTitle { text: "BACKGROUND FILL & EFFECT STYLE"; }
                    ComboBox {
                        height: 34px;
                        model: ["Solid", "Gradient", "Grid", "Aurora", "Noise", "Bokeh", "Starfield", "Nebula", "Psychedelic"];
                        current-value: root.bg-mode;
                        current-index: root.bg-mode == "Solid" ? 0 : root.bg-mode == "Gradient" ? 1 : root.bg-mode == "Grid" ? 2 : root.bg-mode == "Aurora" ? 3 : root.bg-mode == "Noise" ? 4 : root.bg-mode == "Bokeh" ? 5 : root.bg-mode == "Starfield" ? 6 : root.bg-mode == "Nebula" ? 7 : 8;
                        selected(val) => { root.bg-mode = val; root.config-changed(); }
                    }

                    SectionTitle { text: "CUSTOM BACKGROUND IMAGE"; }
                    if (root.custom-bg-name != "") : HorizontalLayout {
                        spacing: 6px;
                        alignment: center;
                        Rectangle {
                            background: #ffd70015;
                            border-radius: 6px;
                            border-width: 1px;
                            border-color: #ffd70040;

                            HorizontalLayout {
                                padding: 6px;
                                spacing: 6px;
                                alignment: center;
                                Text {
                                    text: "📷 Selected: " + root.custom-bg-name;
                                    font-size: 11px;
                                    color: #ffd700;
                                    font-weight: 600;
                                    overflow: elide;
                                    vertical-alignment: center;
                                }
                            }
                        }
                    }

                    HorizontalLayout {
                        spacing: 8px;
                        Button {
                            text: root.custom-bg-name != "" ? "🖼 Change Background Image" : "🖼 Select Background Image";
                            clicked => { root.open-custom-image-clicked(); }
                        }
                        if (root.custom-bg-name != "") : Button {
                            text: "🗑 Remove Image";
                            clicked => { root.remove-custom-image-clicked(); }
                        }
                    }
                    SliderRow {
                        label: "Image Opacity";
                        value-text: Math.round(root.image-opacity * 100) + "%";
                        minimum: 0.0;
                        maximum: 1.0;
                        value: root.image-opacity;
                        changed(val) => { root.image-opacity = val; root.config-changed(); }
                    }

                    SectionTitle { text: "BACKGROUND COLORS"; }

                    if (root.bg-mode == "Gradient") : VerticalLayout {
                        spacing: 10px;

                        ColorPickerField {
                            label: "Gradient Start Color";
                            hex-val: root.gradient-start;
                            changed(val) => { root.gradient-start = val; root.config-changed(); }
                        }

                        ColorPickerField {
                            label: "Gradient End Color";
                            hex-val: root.gradient-end;
                            changed(val) => { root.gradient-end = val; root.config-changed(); }
                        }
                    }

                    if (root.bg-mode != "Gradient") : ColorPickerField {
                        label: "Background Color";
                        hex-val: root.solid-color;
                        changed(val) => { root.solid-color = val; root.config-changed(); }
                    }

                    SliderRow {
                        label: "Canvas Darkening (Overlay)";
                        value-text: Math.round(root.overlay-opacity * 100) + "%";
                        minimum: 0.0;
                        maximum: 1.0;
                        value: root.overlay-opacity;
                        changed(val) => { root.overlay-opacity = val; root.config-changed(); }
                    }

                    // SUB-SLIDERS PER BACKGROUND MODE
                    if (root.bg-mode == "Grid") : VerticalLayout {
                        spacing: 10px;
                        SectionTitle { text: "GRID SETTINGS"; }
                        SliderRow {
                            label: "Grid Size";
                            value-text: Math.round(root.grid-size) + "px";
                            minimum: 10.0;
                            maximum: 120.0;
                            value: root.grid-size;
                            changed(val) => { root.grid-size = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Line Width";
                            value-text: Math.round(root.grid-line-width) + "px";
                            minimum: 1.0;
                            maximum: 6.0;
                            value: root.grid-line-width;
                            changed(val) => { root.grid-line-width = val; root.config-changed(); }
                        }
                        ColorPickerField {
                            label: "Grid Color";
                            hex-val: root.grid-color;
                            changed(val) => { root.grid-color = val; root.config-changed(); }
                        }
                    }

                    if (root.bg-mode == "Starfield") : VerticalLayout {
                        spacing: 10px;
                        SectionTitle { text: "STARFIELD SETTINGS"; }
                        SliderRow {
                            label: "Star Count";
                            value-text: Math.round(root.star-count) + "";
                            minimum: 30.0;
                            maximum: 300.0;
                            value: root.star-count;
                            changed(val) => { root.star-count = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Twinkle Speed";
                            value-text: Math.round(root.star-speed * 100) + "%";
                            minimum: 0.2;
                            maximum: 3.0;
                            value: root.star-speed;
                            changed(val) => { root.star-speed = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Brightness";
                            value-text: Math.round(root.star-brightness * 100) + "%";
                            minimum: 0.2;
                            maximum: 1.5;
                            value: root.star-brightness;
                            changed(val) => { root.star-brightness = val; root.config-changed(); }
                        }
                    }

                    if (root.bg-mode == "Nebula") : VerticalLayout {
                        spacing: 10px;
                        SectionTitle { text: "NEBULA SETTINGS"; }
                        SliderRow {
                            label: "Nebula Intensity";
                            value-text: Math.round(root.nebula-intensity * 100) + "%";
                            minimum: 0.1;
                            maximum: 1.0;
                            value: root.nebula-intensity;
                            changed(val) => { root.nebula-intensity = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Animation Speed";
                            value-text: Math.round(root.nebula-speed * 100) + "%";
                            minimum: 0.2;
                            maximum: 3.0;
                            value: root.nebula-speed;
                            changed(val) => { root.nebula-speed = val; root.config-changed(); }
                        }
                    }

                    if (root.bg-mode == "Aurora") : VerticalLayout {
                        spacing: 10px;
                        SectionTitle { text: "AURORA SETTINGS"; }
                        SliderRow {
                            label: "Wave Speed";
                            value-text: Math.round(root.aurora-speed * 100) + "%";
                            minimum: 0.2;
                            maximum: 3.0;
                            value: root.aurora-speed;
                            changed(val) => { root.aurora-speed = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Wave Height";
                            value-text: Math.round(root.aurora-amplitude) + "px";
                            minimum: 10.0;
                            maximum: 120.0;
                            value: root.aurora-amplitude;
                            changed(val) => { root.aurora-amplitude = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Aurora Opacity";
                            value-text: Math.round(root.aurora-opacity * 100) + "%";
                            minimum: 0.05;
                            maximum: 0.6;
                            value: root.aurora-opacity;
                            changed(val) => { root.aurora-opacity = val; root.config-changed(); }
                        }
                    }

                    if (root.bg-mode == "Noise") : VerticalLayout {
                        spacing: 10px;
                        SectionTitle { text: "FILM GRAIN SETTINGS"; }
                        SliderRow {
                            label: "Grain Opacity";
                            value-text: Math.round(root.grain-opacity * 100) + "%";
                            minimum: 0.01;
                            maximum: 0.3;
                            value: root.grain-opacity;
                            changed(val) => { root.grain-opacity = val; root.config-changed(); }
                        }
                    }

                    if (root.bg-mode == "Bokeh") : VerticalLayout {
                        spacing: 10px;
                        SectionTitle { text: "BOKEH SETTINGS"; }
                        SliderRow {
                            label: "Orb Count";
                            value-text: Math.round(root.bokeh-count) + "";
                            minimum: 5.0;
                            maximum: 40.0;
                            value: root.bokeh-count;
                            changed(val) => { root.bokeh-count = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Orb Size";
                            value-text: Math.round(root.bokeh-size) + "px";
                            minimum: 10.0;
                            maximum: 80.0;
                            value: root.bokeh-size;
                            changed(val) => { root.bokeh-size = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Bokeh Opacity";
                            value-text: Math.round(root.bokeh-opacity * 100) + "%";
                            minimum: 0.05;
                            maximum: 0.6;
                            value: root.bokeh-opacity;
                            changed(val) => { root.bokeh-opacity = val; root.config-changed(); }
                        }
                    }

                    if (root.bg-mode == "Psychedelic") : VerticalLayout {
                        spacing: 10px;
                        SectionTitle { text: "PSYCHEDELIC SETTINGS"; }
                        SliderRow {
                            label: "Spiral Speed";
                            value-text: Math.round(root.psychedelic-speed * 100) + "%";
                            minimum: 0.2;
                            maximum: 3.0;
                            value: root.psychedelic-speed;
                            changed(val) => { root.psychedelic-speed = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Ring Count";
                            value-text: Math.round(root.psychedelic-bands) + "";
                            minimum: 10.0;
                            maximum: 50.0;
                            value: root.psychedelic-bands;
                            changed(val) => { root.psychedelic-bands = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Line Width";
                            value-text: Math.round(root.psychedelic-line-width) + "px";
                            minimum: 1.0;
                            maximum: 12.0;
                            value: root.psychedelic-line-width;
                            changed(val) => { root.psychedelic-line-width = val; root.config-changed(); }
                        }
                    }

                    SectionTitle { text: "FLOATING DECORATIONS"; }
                    CheckBox {
                        text: "Show Floating Particles";
                        checked: root.show-particles;
                        toggled => { root.show-particles = self.checked; root.config-changed(); }
                    }
                    if (root.show-particles) : VerticalLayout {
                        spacing: 10px;
                        Text { text: "Movement Style"; color: #a3a3a3; font-size: 11px; }
                        ComboBox {
                            height: 34px;
                            model: ["Float", "Bounce", "Wave", "Static", "Confined"];
                            current-value: root.particle-style;
                            current-index: root.particle-style == "Float" ? 0 : root.particle-style == "Bounce" ? 1 : root.particle-style == "Wave" ? 2 : root.particle-style == "Static" ? 3 : 4;
                            selected(val) => { root.particle-style = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Particle Count";
                            value-text: Math.round(root.particle-count) + "";
                            minimum: 10.0;
                            maximum: 150.0;
                            value: root.particle-count;
                            changed(val) => { root.particle-count = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Particle Size";
                            value-text: Math.round(root.particle-size) + "px";
                            minimum: 1.0;
                            maximum: 12.0;
                            value: root.particle-size;
                            changed(val) => { root.particle-size = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Reactivity Speed";
                            value-text: Math.round(root.particle-speed * 100) + "%";
                            minimum: 0.2;
                            maximum: 3.0;
                            value: root.particle-speed;
                            changed(val) => { root.particle-speed = val; root.config-changed(); }
                        }
                        ColorPickerField {
                            label: "Particle Color";
                            hex-val: root.particle-color;
                            changed(val) => { root.particle-color = val; root.config-changed(); }
                        }
                    }

                    CheckBox {
                        text: "Show Music Notes";
                        checked: root.show-music-notes;
                        toggled => { root.show-music-notes = self.checked; root.config-changed(); }
                    }
                    if (root.show-music-notes) : VerticalLayout {
                        spacing: 10px;
                        Text { text: "Movement Style"; color: #a3a3a3; font-size: 11px; }
                        ComboBox {
                            height: 34px;
                            model: ["Float", "Bounce", "Spiral", "Wave", "Burst", "Confined"];
                            current-value: root.music-note-style;
                            current-index: root.music-note-style == "Float" ? 0 : root.music-note-style == "Bounce" ? 1 : root.music-note-style == "Spiral" ? 2 : root.music-note-style == "Wave" ? 3 : root.music-note-style == "Burst" ? 4 : 5;
                            selected(val) => { root.music-note-style = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Max Note Count";
                            value-text: Math.round(root.music-note-count) + "";
                            minimum: 10.0;
                            maximum: 80.0;
                            value: root.music-note-count;
                            changed(val) => { root.music-note-count = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Note Size";
                            value-text: Math.round(root.music-note-size) + "px";
                            minimum: 16.0;
                            maximum: 120.0;
                            value: root.music-note-size;
                            changed(val) => { root.music-note-size = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Beat Sensitivity";
                            value-text: Math.round(root.music-note-sensitivity * 100) + "%";
                            minimum: 0.0;
                            maximum: 2.0;
                            value: root.music-note-sensitivity;
                            changed(val) => { root.music-note-sensitivity = val; root.config-changed(); }
                        }
                        ColorPickerField {
                            label: "Note Color";
                            hex-val: root.music-note-color;
                            changed(val) => { root.music-note-color = val; root.config-changed(); }
                        }
                    }

                    CheckBox {
                        text: "Show Fireworks & Starburst Constellations";
                        checked: root.show-fireworks;
                        toggled => { root.show-fireworks = self.checked; root.config-changed(); }
                    }
                    if (root.show-fireworks) : VerticalLayout {
                        spacing: 10px;
                        SliderRow {
                            label: "Burst Count";
                            value-text: Math.round(root.fireworks-count) + "";
                            minimum: 3.0;
                            maximum: 20.0;
                            value: root.fireworks-count;
                            changed(val) => { root.fireworks-count = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Spark Size";
                            value-text: Math.round(root.fireworks-size) + "px";
                            minimum: 1.0;
                            maximum: 12.0;
                            value: root.fireworks-size;
                            changed(val) => { root.fireworks-size = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Expansion Speed";
                            value-text: Math.round(root.fireworks-speed * 100) + "%";
                            minimum: 0.2;
                            maximum: 3.0;
                            value: root.fireworks-speed;
                            changed(val) => { root.fireworks-speed = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "3D Perspective Depth";
                            value-text: Math.round(root.fireworks-depth * 100) + "%";
                            minimum: 0.5;
                            maximum: 3.0;
                            value: root.fireworks-depth;
                            changed(val) => { root.fireworks-depth = val; root.config-changed(); }
                        }
                        ColorPickerField {
                            label: "Fireworks Color";
                            hex-val: root.fireworks-color;
                            changed(val) => { root.fireworks-color = val; root.config-changed(); }
                        }
                    }

                    CheckBox {
                        text: "Show Matrix Code Rain";
                        checked: root.show-matrix-rain;
                        toggled => { root.show-matrix-rain = self.checked; root.config-changed(); }
                    }
                    if (root.show-matrix-rain) : VerticalLayout {
                        spacing: 10px;
                        SliderRow {
                            label: "Column Count";
                            value-text: Math.round(root.matrix-rain-count) + "";
                            minimum: 10.0;
                            maximum: 90.0;
                            value: root.matrix-rain-count;
                            changed(val) => { root.matrix-rain-count = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Glyph Size";
                            value-text: Math.round(root.matrix-rain-size) + "px";
                            minimum: 2.0;
                            maximum: 12.0;
                            value: root.matrix-rain-size;
                            changed(val) => { root.matrix-rain-size = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Fall Speed";
                            value-text: Math.round(root.matrix-rain-speed * 100) + "%";
                            minimum: 0.2;
                            maximum: 3.0;
                            value: root.matrix-rain-speed;
                            changed(val) => { root.matrix-rain-speed = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "3D Grid Depth";
                            value-text: Math.round(root.matrix-rain-depth * 100) + "%";
                            minimum: 0.5;
                            maximum: 3.0;
                            value: root.matrix-rain-depth;
                            changed(val) => { root.matrix-rain-depth = val; root.config-changed(); }
                        }
                        ColorPickerField {
                            label: "Matrix Color";
                            hex-val: root.matrix-rain-color;
                            changed(val) => { root.matrix-rain-color = val; root.config-changed(); }
                        }
                    }

                    CheckBox {
                        text: "Show Fireflies & Magic Dust";
                        checked: root.show-fireflies;
                        toggled => { root.show-fireflies = self.checked; root.config-changed(); }
                    }
                    if (root.show-fireflies) : VerticalLayout {
                        spacing: 10px;
                        SliderRow {
                            label: "Firefly Count";
                            value-text: Math.round(root.fireflies-count) + "";
                            minimum: 10.0;
                            maximum: 100.0;
                            value: root.fireflies-count;
                            changed(val) => { root.fireflies-count = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Glow Size";
                            value-text: Math.round(root.fireflies-size) + "px";
                            minimum: 2.0;
                            maximum: 16.0;
                            value: root.fireflies-size;
                            changed(val) => { root.fireflies-size = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Drift Speed";
                            value-text: Math.round(root.fireflies-speed * 100) + "%";
                            minimum: 0.2;
                            maximum: 3.0;
                            value: root.fireflies-speed;
                            changed(val) => { root.fireflies-speed = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "3D Swarm Depth";
                            value-text: Math.round(root.fireflies-depth * 100) + "%";
                            minimum: 0.5;
                            maximum: 3.0;
                            value: root.fireflies-depth;
                            changed(val) => { root.fireflies-depth = val; root.config-changed(); }
                        }
                        ColorPickerField {
                            label: "Firefly Color";
                            hex-val: root.fireflies-color;
                            changed(val) => { root.fireflies-color = val; root.config-changed(); }
                        }
                    }

                    CheckBox {
                        text: "Show Floating Sakura Petals";
                        checked: root.show-sakura;
                        toggled => { root.show-sakura = self.checked; root.config-changed(); }
                    }
                    if (root.show-sakura) : VerticalLayout {
                        spacing: 10px;
                        SliderRow {
                            label: "Petal Count";
                            value-text: Math.round(root.sakura-count) + "";
                            minimum: 10.0;
                            maximum: 80.0;
                            value: root.sakura-count;
                            changed(val) => { root.sakura-count = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Petal Size";
                            value-text: Math.round(root.sakura-size) + "px";
                            minimum: 4.0;
                            maximum: 24.0;
                            value: root.sakura-size;
                            changed(val) => { root.sakura-size = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Fall Speed";
                            value-text: Math.round(root.sakura-speed * 100) + "%";
                            minimum: 0.2;
                            maximum: 3.0;
                            value: root.sakura-speed;
                            changed(val) => { root.sakura-speed = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "3D Tumbling Spin Rate";
                            value-text: Math.round(root.sakura-depth * 100) + "%";
                            minimum: 0.5;
                            maximum: 3.0;
                            value: root.sakura-depth;
                            changed(val) => { root.sakura-depth = val; root.config-changed(); }
                        }
                        ColorPickerField {
                            label: "Sakura Color";
                            hex-val: root.sakura-color;
                            changed(val) => { root.sakura-color = val; root.config-changed(); }
                        }
                    }

                    CheckBox {
                        text: "Show Cyber Lightning Arcs";
                        checked: root.show-cyber-lightning;
                        toggled => { root.show-cyber-lightning = self.checked; root.config-changed(); }
                    }
                    if (root.show-cyber-lightning) : VerticalLayout {
                        spacing: 10px;
                        SliderRow {
                            label: "Arc Count";
                            value-text: Math.round(root.cyber-lightning-count) + "";
                            minimum: 2.0;
                            maximum: 12.0;
                            value: root.cyber-lightning-count;
                            changed(val) => { root.cyber-lightning-count = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Arc Thickness";
                            value-text: Math.round(root.cyber-lightning-size) + "px";
                            minimum: 1.0;
                            maximum: 8.0;
                            value: root.cyber-lightning-size;
                            changed(val) => { root.cyber-lightning-size = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "Flash Speed";
                            value-text: Math.round(root.cyber-lightning-speed * 100) + "%";
                            minimum: 0.2;
                            maximum: 3.0;
                            value: root.cyber-lightning-speed;
                            changed(val) => { root.cyber-lightning-speed = val; root.config-changed(); }
                        }
                        SliderRow {
                            label: "3D Mesh Radius";
                            value-text: Math.round(root.cyber-lightning-depth * 100) + "%";
                            minimum: 0.5;
                            maximum: 3.0;
                            value: root.cyber-lightning-depth;
                            changed(val) => { root.cyber-lightning-depth = val; root.config-changed(); }
                        }
                        ColorPickerField {
                            label: "Lightning Color";
                            hex-val: root.cyber-lightning-color;
                            changed(val) => { root.cyber-lightning-color = val; root.config-changed(); }
                        }
                    }
                }

                // EFFECTS TAB
                if (root.active-tab == "effects") : VerticalLayout {
                    alignment: start;
                    spacing: 10px;
                    SectionTitle { text: "SCREEN EFFECTS"; }

                    CheckBox {
                        text: "Enable Effects";
                        checked: root.effects-enabled;
                        toggled => { root.effects-enabled = self.checked; root.config-changed(); }
                    }

                    if (root.effects-enabled) : VerticalLayout {
                        alignment: start;
                        spacing: 10px;
                        CheckBox {
                            text: "Apply Effects to Background Only";
                            checked: root.bg-only-effect;
                            toggled => { root.bg-only-effect = self.checked; root.config-changed(); }
                        }

                        Text { text: "Effect Type"; color: #a3a3a3; font-size: 11px; }
                        ComboBox {
                            height: 34px;
                            model: ["None", "Shake", "Glitch", "Vignette", "Pulse", "Spotlight", "Strobe", "Scanline", "Chromatic Aberration", "Zoom", "Invert", "Bars", "Shockwave", "Pixelate", "Tilt", "Heat Haze", "Hue Shift"];
                            current-value: root.main-effect;
                            current-index: root.main-effect == "None" ? 0 : root.main-effect == "Shake" ? 1 : root.main-effect == "Glitch" ? 2 : root.main-effect == "Vignette" ? 3 : root.main-effect == "Pulse" ? 4 : root.main-effect == "Spotlight" ? 5 : root.main-effect == "Strobe" ? 6 : root.main-effect == "Scanline" ? 7 : root.main-effect == "Chromatic Aberration" ? 8 : root.main-effect == "Zoom" ? 9 : root.main-effect == "Invert" ? 10 : root.main-effect == "Bars" ? 11 : root.main-effect == "Shockwave" ? 12 : root.main-effect == "Pixelate" ? 13 : root.main-effect == "Tilt" ? 14 : root.main-effect == "Heat Haze" ? 15 : 16;
                            selected(val) => { root.main-effect = val; root.config-changed(); }
                        }

                        Text {
                            text: root.main-effect == "Shake" ? "Canvas vibrates with the beat (background parallax depth)"
                                : root.main-effect == "Glitch" ? "Random slice displacement & color artifacts"
                                : root.main-effect == "Vignette" ? "Edges darken dynamically on bass hits"
                                : root.main-effect == "Pulse" ? "Bright flash on each beat"
                                : root.main-effect == "Spotlight" ? "Concert light beams from above"
                                : root.main-effect == "Strobe" ? "Rapid white flashes synced to the music"
                                : root.main-effect == "Scanline" ? "Retro TV scanlines that darken with the beat"
                                : root.main-effect == "Chromatic Aberration" ? "RGB color-split ghosting on bass"
                                : root.main-effect == "Zoom" ? "Canvas zooms in on each beat"
                                : root.main-effect == "Invert" ? "Colors briefly invert on bass hits"
                                : root.main-effect == "Bars" ? "Letterbox bars push in on bass"
                                : root.main-effect == "Shockwave" ? "Radial ripple distortion from the center on each beat"
                                : root.main-effect == "Pixelate" ? "Frame drops to chunky blocks on bass"
                                : root.main-effect == "Tilt" ? "Canvas tilts back & forth with the music"
                                : root.main-effect == "Heat Haze" ? "Wavy air distortion shimmering on bass"
                                : root.main-effect == "Hue Shift" ? "Colors drift continuously through the spectrum"
                                : "No active screen effect";
                            color: #737373;
                            font-size: 11px;
                        }

                        if (root.main-effect == "Shake") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Shake Intensity";
                                value-text: Math.round(root.shake-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 3.0;
                                value: root.shake-intensity;
                                changed(val) => { root.shake-intensity = val; root.config-changed(); }
                            }
                            SliderRow {
                                label: "Shake Frequency";
                                value-text: Math.round(root.shake-frequency * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.shake-frequency;
                                changed(val) => { root.shake-frequency = val; root.config-changed(); }
                            }
                            SliderRow {
                                label: "Max Offset";
                                value-text: Math.round(root.shake-max-offset) + "px";
                                minimum: 5.0;
                                maximum: 120.0;
                                value: root.shake-max-offset;
                                changed(val) => { root.shake-max-offset = Math.round(val); root.config-changed(); }
                            }
                            CheckBox {
                                text: "Shake only on beats";
                                checked: root.shake-on-beat;
                                toggled => { root.shake-on-beat = self.checked; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Glitch") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Glitch Intensity";
                                value-text: Math.round(root.glitch-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.glitch-intensity;
                                changed(val) => { root.glitch-intensity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Vignette") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Vignette Intensity";
                                value-text: Math.round(root.vignette-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.vignette-intensity;
                                changed(val) => { root.vignette-intensity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Pulse") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Pulse Strength";
                                value-text: Math.round(root.pulse-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.pulse-intensity;
                                changed(val) => { root.pulse-intensity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Spotlight") : VerticalLayout {
                            spacing: 10px;
                            ColorPickerField {
                                label: "Spotlight Color";
                                hex-val: root.spotlight-color;
                                changed(val) => { root.spotlight-color = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Strobe") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Strobe Intensity";
                                value-text: Math.round(root.strobe-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.strobe-intensity;
                                changed(val) => { root.strobe-intensity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Scanline") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Scanline Opacity";
                                value-text: Math.round(root.scanline-opacity * 100) + "%";
                                minimum: 0.0;
                                maximum: 0.5;
                                value: root.scanline-opacity;
                                changed(val) => { root.scanline-opacity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Chromatic Aberration") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Chromatic Intensity";
                                value-text: Math.round(root.chromatic-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.chromatic-intensity;
                                changed(val) => { root.chromatic-intensity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Zoom") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Zoom Intensity";
                                value-text: Math.round(root.zoom-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 0.5;
                                value: root.zoom-intensity;
                                changed(val) => { root.zoom-intensity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Invert") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Invert Intensity";
                                value-text: Math.round(root.invert-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.invert-intensity;
                                changed(val) => { root.invert-intensity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Bars") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Bars Amount";
                                value-text: Math.round(root.bars-amount * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.bars-amount;
                                changed(val) => { root.bars-amount = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Shockwave") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Shockwave Intensity";
                                value-text: Math.round(root.shockwave-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.shockwave-intensity;
                                changed(val) => { root.shockwave-intensity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Pixelate") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Pixelate Intensity";
                                value-text: Math.round(root.pixelate-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.pixelate-intensity;
                                changed(val) => { root.pixelate-intensity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Tilt") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Tilt Intensity";
                                value-text: Math.round(root.tilt-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.tilt-intensity;
                                changed(val) => { root.tilt-intensity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Heat Haze") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Heat Haze Intensity";
                                value-text: Math.round(root.heat-haze-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.heat-haze-intensity;
                                changed(val) => { root.heat-haze-intensity = val; root.config-changed(); }
                            }
                        }

                        if (root.main-effect == "Hue Shift") : VerticalLayout {
                            spacing: 10px;
                            SliderRow {
                                label: "Hue Shift Intensity";
                                value-text: Math.round(root.hue-shift-intensity * 100) + "%";
                                minimum: 0.0;
                                maximum: 1.0;
                                value: root.hue-shift-intensity;
                                changed(val) => { root.hue-shift-intensity = val; root.config-changed(); }
                            }
                        }
                    }
                }

                // TEXT OVERLAY TAB
                if (root.active-tab == "text") : VerticalLayout {
                    spacing: 10px;
                    SectionTitle { text: "SONG METADATA & TYPOGRAPHY"; }

                    CheckBox {
                        text: "Display Song Title";
                        checked: root.show-title;
                        toggled => { root.show-title = self.checked; root.config-changed(); }
                    }

                    LineEdit {
                        text: root.track-title;
                        placeholder-text: "Song Title";
                        edited(val) => { root.track-title = val; root.config-changed(); }
                    }

                    CheckBox {
                        text: "Display Artist Name";
                        checked: root.show-artist;
                        toggled => { root.show-artist = self.checked; root.config-changed(); }
                    }

                    LineEdit {
                        text: root.track-artist;
                        placeholder-text: "Artist Name";
                        edited(val) => { root.track-artist = val; root.config-changed(); }
                    }

                    SectionTitle { text: "DYNAMIC CUSTOM TEXT OVERLAYS"; }

                    Button {
                        text: "+ Add Custom Text";
                        height: 36px;
                        clicked => { root.add-custom-text-clicked(); }
                    }

                    for block[idx] in root.custom-text-blocks : Rectangle {
                        background: #ffffff0a;
                        border-radius: 8px;
                        border-width: 1px;
                        border-color: #ffd70030;

                        VerticalLayout {
                            padding: 10px;
                            spacing: 8px;

                            HorizontalLayout {
                                spacing: 8px;
                                alignment: center;
                                CheckBox {
                                    text: "Enable Text #" + (idx + 1);
                                    checked: block.enabled;
                                    toggled => {
                                        root.custom-text-edited(block.id, block.text, self.checked, block.pos_x, block.pos_y, block.font_size, block.font_family, block.font_weight, block.color_hex);
                                    }
                                }
                                Rectangle { horizontal-stretch: 1.0; }
                                Button {
                                    text: "Remove";
                                    height: 28px;
                                    clicked => { root.remove-custom-text-clicked(block.id); }
                                }
                            }

                            LineEdit {
                                text: block.text;
                                placeholder-text: "Custom Text (e.g. CredibleMark Studio)";
                                edited(val) => {
                                    root.custom-text-edited(block.id, val, block.enabled, block.pos_x, block.pos_y, block.font_size, block.font_family, block.font_weight, block.color_hex);
                                }
                            }

                            HorizontalLayout {
                                spacing: 10px;
                                Text {
                                    text: "Font Variant:";
                                    color: #a3a3a3;
                                    font-size: 11px;
                                    vertical-alignment: center;
                                }
                                ComboBox {
                                    height: 30px;
                                    horizontal-stretch: 1.0;
                                    model: ["Inter", "Roboto", "Outfit", "Serif", "Monospace"];
                                    current-value: block.font_family;
                                    selected(val) => {
                                        root.custom-text-edited(block.id, block.text, block.enabled, block.pos_x, block.pos_y, block.font_size, val, block.font_weight, block.color_hex);
                                    }
                                }
                            }

                            HorizontalLayout {
                                spacing: 10px;
                                Text {
                                    text: "Font Weight:";
                                    color: #a3a3a3;
                                    font-size: 11px;
                                    vertical-alignment: center;
                                }
                                ComboBox {
                                    height: 30px;
                                    horizontal-stretch: 1.0;
                                    model: ["Normal", "Bold", "Heavy"];
                                    current-value: block.font_weight;
                                    selected(val) => {
                                        root.custom-text-edited(block.id, block.text, block.enabled, block.pos_x, block.pos_y, block.font_size, block.font_family, val, block.color_hex);
                                    }
                                }
                            }

                            SliderRow {
                                label: "Pos X";
                                value-text: Math.round(block.pos_x) + "%";
                                minimum: 0.0;
                                maximum: 100.0;
                                value: block.pos_x;
                                changed(val) => {
                                    root.custom-text-edited(block.id, block.text, block.enabled, val, block.pos_y, block.font_size, block.font_family, block.font_weight, block.color_hex);
                                }
                            }

                            SliderRow {
                                label: "Pos Y";
                                value-text: Math.round(block.pos_y) + "%";
                                minimum: 0.0;
                                maximum: 100.0;
                                value: block.pos_y;
                                changed(val) => {
                                    root.custom-text-edited(block.id, block.text, block.enabled, block.pos_x, val, block.font_size, block.font_family, block.font_weight, block.color_hex);
                                }
                            }

                            SliderRow {
                                label: "Font Size";
                                value-text: Math.round(block.font_size) + "px";
                                minimum: 10.0;
                                maximum: 120.0;
                                value: block.font_size;
                                changed(val) => {
                                    root.custom-text-edited(block.id, block.text, block.enabled, block.pos_x, block.pos_y, val, block.font_family, block.font_weight, block.color_hex);
                                }
                            }
                        }
                    }

                    SectionTitle { text: "QUICK POSITION PRESET"; }
                    ComboBox {
                        height: 34px;
                        model: ["Bottom Center", "Top Center", "Center", "Bottom Left", "Left Middle", "Right Middle"];
                        current-value: root.text-preset;
                        current-index: root.text-preset == "Bottom Center" ? 0 : root.text-preset == "Top Center" ? 1 : root.text-preset == "Center" ? 2 : root.text-preset == "Bottom Left" ? 3 : root.text-preset == "Left Middle" ? 4 : 5;
                        selected(val) => {
                            root.text-preset = val;
                            if (val == "Bottom Center") {
                                root.title-pos-x = 50.0; root.title-pos-y = 78.0;
                                root.artist-pos-x = 50.0; root.artist-pos-y = 86.0;
                            } else if (val == "Top Center") {
                                root.title-pos-x = 50.0; root.title-pos-y = 12.0;
                                root.artist-pos-x = 50.0; root.artist-pos-y = 20.0;
                            } else if (val == "Center") {
                                root.title-pos-x = 50.0; root.title-pos-y = 45.0;
                                root.artist-pos-x = 50.0; root.artist-pos-y = 55.0;
                            } else if (val == "Bottom Left") {
                                root.title-pos-x = 8.0; root.title-pos-y = 78.0;
                                root.artist-pos-x = 8.0; root.artist-pos-y = 86.0;
                            } else if (val == "Left Middle") {
                                root.title-pos-x = 8.0; root.title-pos-y = 45.0;
                                root.artist-pos-x = 8.0; root.artist-pos-y = 55.0;
                            } else if (val == "Right Middle") {
                                root.title-pos-x = 92.0; root.title-pos-y = 45.0;
                                root.artist-pos-x = 92.0; root.artist-pos-y = 55.0;
                            }
                            root.config-changed();
                        }
                    }

                    SectionTitle { text: "TITLE STYLE & POSITION"; }
                    SliderRow {
                        label: "Title Horizontal X";
                        value-text: Math.round(root.title-pos-x) + "%";
                        minimum: 0.0;
                        maximum: 100.0;
                        value: root.title-pos-x;
                        changed(val) => { root.title-pos-x = Math.round(val); root.config-changed(); }
                    }
                    SliderRow {
                        label: "Title Vertical Y";
                        value-text: Math.round(root.title-pos-y) + "%";
                        minimum: 0.0;
                        maximum: 100.0;
                        value: root.title-pos-y;
                        changed(val) => { root.title-pos-y = Math.round(val); root.config-changed(); }
                    }
                    SliderRow {
                        label: "Title Font Size";
                        value-text: Math.round(root.title-font-size) + "px";
                        minimum: 12.0;
                        maximum: 96.0;
                        value: root.title-font-size;
                        changed(val) => { root.title-font-size = Math.round(val); root.config-changed(); }
                    }
                    ColorPickerField {
                        label: "Title Color";
                        hex-val: root.title-color;
                        changed(val) => { root.title-color = val; root.config-changed(); }
                    }

                    SectionTitle { text: "ARTIST STYLE & POSITION"; }
                    SliderRow {
                        label: "Artist Horizontal X";
                        value-text: Math.round(root.artist-pos-x) + "%";
                        minimum: 0.0;
                        maximum: 100.0;
                        value: root.artist-pos-x;
                        changed(val) => { root.artist-pos-x = Math.round(val); root.config-changed(); }
                    }
                    SliderRow {
                        label: "Artist Vertical Y";
                        value-text: Math.round(root.artist-pos-y) + "%";
                        minimum: 0.0;
                        maximum: 100.0;
                        value: root.artist-pos-y;
                        changed(val) => { root.artist-pos-y = Math.round(val); root.config-changed(); }
                    }
                    SliderRow {
                        label: "Artist Font Size";
                        value-text: Math.round(root.artist-font-size) + "px";
                        minimum: 10.0;
                        maximum: 72.0;
                        value: root.artist-font-size;
                        changed(val) => { root.artist-font-size = Math.round(val); root.config-changed(); }
                    }
                    ColorPickerField {
                        label: "Artist Color";
                        hex-val: root.artist-color;
                        changed(val) => { root.artist-color = val; root.config-changed(); }
                    }
                }
            }
        }
    }
}
