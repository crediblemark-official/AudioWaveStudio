export struct CustomTextBlockData {
    id: int,
    text: string,
    enabled: bool,
    pos_x: float,
    pos_y: float,
    font_size: float,
    font_family: string,
    font_weight: string,
    color_hex: string,
}
